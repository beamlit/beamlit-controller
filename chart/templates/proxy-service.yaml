apiVersion: v1
kind: Service
metadata:
  name: proxy-svc
  labels:
    app: proxy
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    control-plane: controller-manager
